"use strict";

describe("Auth — Login (integration)", function () {
    it.skip("POST /api/v1/auth/login — auth routes are not yet mounted (commented out in routes/index.js)", function () {});
    it.skip("returns 401 for invalid credentials", function () {});
    it.skip("returns 200 with token for valid credentials", function () {});
    it.skip("returns 400 for missing username or password", function () {});
});
