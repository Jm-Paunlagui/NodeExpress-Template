"use strict";

describe("Auth — Logout (integration)", function () {
    it.skip("POST /api/v1/auth/logout — auth routes are not yet mounted (commented out in routes/index.js)", function () {});
    it.skip("returns 200 and clears the token cookie", function () {});
    it.skip("returns 401 when called without a valid session", function () {});
});
