"use strict";

describe("Auth — Refresh (integration)", function () {
    it.skip("POST /api/v1/auth/refresh — auth routes are not yet mounted (commented out in routes/index.js)", function () {});
    it.skip("returns a new access token when given a valid refresh token", function () {});
    it.skip("returns 401 for expired refresh token", function () {});
});
