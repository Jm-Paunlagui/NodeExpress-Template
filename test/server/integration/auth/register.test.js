"use strict";

describe("Auth — Register (integration)", function () {
    it.skip("POST /api/v1/auth/register — auth routes are not yet mounted (commented out in routes/index.js)", function () {});
    it.skip("returns 201 for valid registration data", function () {});
    it.skip("returns 400 for duplicate username", function () {});
    it.skip("returns 400 for missing required fields", function () {});
});
